/** URL base da API. Ajuste por ambiente (ex.: variável do EAS / app config). */
export const API_BASE_URL = process.env.EXPO_PUBLIC_API_URL ?? 'https://sistemalocacao-api-production.up.railway.app';
